# LangGraph 核心依赖
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0

# HTTP 客户端
httpx>=0.24.0
requests>=2.31.0

# API 服务器
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# 环境变量
python-dotenv>=1.0.0

# OpenAI 客户端
openai

# 数据库 - PostgreSQL
psycopg2-binary>=2.9.0
psycopg[binary]>=3.1.0          # LangGraph checkpoint 专用（psycopg3，与 psycopg2 共存）
langgraph-checkpoint-postgres>=2.0.0

# 向量数据库 - Milvus
pymilvus>=2.5.0

# Token counting
tiktoken>=0.5.0

# 加密（namespace 密码加密保留）
cryptography>=45.0.7,<47
bcrypt>=4.0.0
PyJWT>=2.8.0

# 文件上传
python-multipart

# 阿里云服务（Embedding / Rerank / 多模态嵌入保留用）
dashscope
oss2
alibabacloud_oss_v2

# 火山引擎多模态嵌入通过 httpx 直接调用 REST API（无需额外 SDK）

# 文档解析
pageindex
pymupdf
python-docx
pandas
openpyxl
